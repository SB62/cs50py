# Ask user for their name
name = input("What's your name? ").strip().title()

# Strip whitespace and capitalize first letters of Input
name = name.strip().title()

# Say hello to user
print(f"hello, {name}")
